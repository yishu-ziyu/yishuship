You must use Skill('yishuship:refactor').
Skip preamble and auth gate.

Do behavior-preserving cleanup on files changed in this task.
After making changes, run the repo's test command to verify nothing broke.
If tests fail after your changes, revert them.

You MUST always write {{TASK_DIR}}/refactor.md with a summary of what was done.
Even if no changes were needed, write a brief explanation of why the code is already clean.

Scope: files changed on this branch (use git diff origin/HEAD...HEAD --name-only)
task_dir: {{TASK_DIR}}
Mode: /yishuship:auto staged workflow — no user questions.
